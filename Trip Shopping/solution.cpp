#include <bits/stdc++.h>

using namespace std;

int main()
{
    int num_test_cases;
    cin >> num_test_cases;

    for (int i = 0; i < num_test_cases; i++)
    {
        int n, k;
        cin >> n >> k;
        int a[n];
        int b[k];
        for (int j = 0; j < n; j++)
        {
            cin >> a[j];
        }
        for (int j = 0; j < n; j++)
        {
            cin >> b[j];
        }
    }
}